.cursor/
CLAUDE.md
.vscode/
pydantic/.env
.DS_Store

# Claude Code local config
.claude/*
# ...but share project commands with the team; only local settings stay out.
!.claude/commands/

frontend/public/four in a row original codebase/*