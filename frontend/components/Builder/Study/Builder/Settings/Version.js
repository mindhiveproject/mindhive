// The study versions panel is shared with the project builder, so that both
// save paths show and manage the same version history.
export { default } from "../../../Project/Builder/Settings/Version";
