import styled from "styled-components";

export const StyledBank = styled.div`
  display: grid;
  justify-content: center;
  margin: 0 auto;
  grid-template-columns: (auto-fill, minmax(315px, 1fr));
  grid-gap: 20px;
  margin: 20px;
`;
